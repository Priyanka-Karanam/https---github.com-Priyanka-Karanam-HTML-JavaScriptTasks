class Vehicle{

    constructor(name,color){ 
        this.name=name; 
        this.color=color;
    }
    }
    
    const obj=new Vehicle('Tata','Blue'); 
    const obj2=new Vehicle('Duke','Black');
    console.log(obj2.name); 
    console.log(obj.color);
    