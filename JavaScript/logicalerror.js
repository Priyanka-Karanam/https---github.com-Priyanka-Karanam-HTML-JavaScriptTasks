// let arr = [1, 2, 3, 4, 5];
// for (let i = 0; i <= arr.length; i++) { try {
// // Attempt to access the array element console.log(arr[i]);
// } catch (error) 
// {
// // Catch any errors	undefined array index console.log("An error occurred: " + error.message);
// } finally {

// // This block runs whether an error occurs or not console.log("Iteration", i, "complete.");
// }
// }
//EX2
function sumNumbers() {
    //let a=10;
    
    //let b=15;
    
    console.log(a + b); // Logical error: `a` and `b` are used before they are declared
    
    let a = 10;
    
    let b = 15;
    
    }
    sumNumbers(); // This will throw a ReferenceError
    
    